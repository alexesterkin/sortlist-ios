<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Sortlist</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>1.1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionAttributes</key>
		<dict>
			<key>NSExtensionActivationRule</key>
			<dict>
				<!-- Triggered when the host (e.g. Safari) hands us a URL value
				     directly, e.g. from a Copy Link context. -->
				<key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
				<integer>1</integer>
				<!-- Triggered when the user shares from a web page in Safari.
				     This is the path that ALSO runs our JS preprocessor below.
				     Without this rule the preprocessor never fires and we lose
				     the real window.location.href + local OG scrape. -->
				<key>NSExtensionActivationSupportsWebPageWithMaxCount</key>
				<integer>1</integer>
				<key>NSExtensionActivationSupportsText</key>
				<true/>
			</dict>
			<!-- Filename (no extension) of the bundled JS preprocessor.
			     ShareExtensionPreprocessor.js is in the .appex's Resources
			     phase; Safari loads it into the page and calls
			     ExtensionPreprocessingJS.run({completionFunction}) right
			     before activating the extension. The returned dict is
			     delivered to Swift via a kUTTypePropertyList attachment. -->
			<key>NSExtensionJavaScriptPreprocessingFile</key>
			<string>ShareExtensionPreprocessor</string>
		</dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
	</dict>
	<!--
	  iOS 9+ requires every URL scheme that an app extension might open
	  (via extensionContext.open or canOpenURL) to be declared here.
	  Build 14's diagnostic confirmed extensionContext.open(sortlist://…)
	  returned false even though the parent app correctly registers the
	  scheme — the extension is a separate bundle and inherits NONE of
	  the parent's CFBundleURLTypes for purposes of open().
	  We declare both the custom sortlist scheme (in case Apple ever
	  un-broke that path) and https — Build 15 pivots to Universal
	  Links as the primary deep-link, which goes via the https scheme
	  and is far more reliable.
	-->
	<key>LSApplicationQueriesSchemes</key>
	<array>
		<string>sortlist</string>
		<string>https</string>
	</array>
</dict>
</plist>
