<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Sortlist</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>1.1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionAttributes</key>
		<dict>
			<key>NSExtensionActivationRule</key>
			<dict>
				<!-- URL activation — Safari "Share" button, Copy Link, any
				     other app that shares a URL. This is now our ONLY
				     activation channel for webpage shares; we removed
				     NSExtensionActivationSupportsWebPageWithMaxCount + the
				     JS preprocessor in v1.1 build 31 because preprocessor
				     mode silently dropped the public.url attachment that
				     URL mode reliably delivers — Safari delivers EITHER
				     the preprocessor result OR public.url for a given
				     share, not both, and the preprocessor returned no URL
				     on heavy-CSP retailers like M&S. URL-only mode is
				     the same channel Messages / WhatsApp / Mail / Notes
				     use; it's been bulletproof for them. We trade
				     instant in-page metadata scraping (gone) for
				     universal URL extraction. Backend meta.fetch +
				     OnDeviceScraper fill in title/image/price asynchronously
				     after the URL is in hand. -->
				<key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
				<integer>1</integer>
				<key>NSExtensionActivationSupportsText</key>
				<true/>
			</dict>
		</dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
	</dict>
	<!--
	  iOS 9+ requires every URL scheme that an app extension might open
	  (via extensionContext.open or canOpenURL) to be declared here.
	  Build 14's diagnostic confirmed extensionContext.open(sortlist://…)
	  returned false even though the parent app correctly registers the
	  scheme — the extension is a separate bundle and inherits NONE of
	  the parent's CFBundleURLTypes for purposes of open().
	  We declare both the custom sortlist scheme (in case Apple ever
	  un-broke that path) and https — Build 15 pivots to Universal
	  Links as the primary deep-link, which goes via the https scheme
	  and is far more reliable.
	-->
	<key>LSApplicationQueriesSchemes</key>
	<array>
		<string>sortlist</string>
		<string>https</string>
	</array>
</dict>
</plist>
